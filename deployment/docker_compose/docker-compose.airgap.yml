name: onyx

# =============================================================================
# 폐쇄망(Air-Gap) 배포 오버라이드
#
# 패키지 배포 시 경로 교정은 package.sh에서 docker-compose.prod.yml의
# ../data/ 를 ./data/ 로 직접 치환하므로, 이 파일에서는 별도 처리 불필요.
#
# 이 파일은 향후 폐쇄망 전용 추가 설정을 위해 유지합니다.
# =============================================================================
